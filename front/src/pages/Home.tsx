import { Container, Typography } from "@mui/material";

const Home = (_props: unknown) => {
  return (
    <Container>
      <Typography variant="h4">HOME</Typography>
    </Container>
  );
};

export default Home;
